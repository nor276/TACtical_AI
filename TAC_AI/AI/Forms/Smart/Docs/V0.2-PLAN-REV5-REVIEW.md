# V0.2-PLAN-REV5 Adversarial Review Reconciliation

## 1. Verdict Tally

**10/10 NEEDS-REVISION. 0/10 SHIP. 0/10 SHIP-WITH-MINOR-FIXES.**

Unanimous rejection. REV 5 partially recovered from REV 4's 0/10 collapse (Item 27 is now genuinely sound, Item 24 still clean) but reintroduced the **exact discipline failure** â€” phantom-API hedges with "verify at impl time" punts â€” that sank REV 4. Two of the four flagship restorations (Items 22, 26) ship as compiled-but-functionally-inert features, and Item 28 was left entirely unaddressed despite carrying the same class of phantoms REV 5 fixed for its siblings.

---

## 2. Restoration Audit

| Item | SHIPS-AS-DESIGNED | SHIPS-WITH-MINOR-FIXES | BLOCKED | Consensus |
|------|:---:|:---:|:---:|---|
| **22** (TeamBelief LOS facade) | 0 | 1 | 9 | **BLOCKED** |
| **24** (WPerimeterCoverage) | 10 | 0 | 0 | **SHIPS-AS-DESIGNED** |
| **26** (RepairSupport via HealthSidecar) | 0 | 2 | 8 | **BLOCKED** |
| **27** (GenericGoalSource via handle) | 6 | 4 | 0 | **SHIPS-AS-DESIGNED / minor fix** |

### Per-item evidence

**Item 22 â€” BLOCKED (9/10)**
Three convergent phantoms:
1. `SmartRuntime.Teams` â€” does not exist. Real API: `internal static IEnumerable<TeamRuntime> EnumerateTeams()` at SmartRuntime.cs:657.
2. `TeamRuntime.TeamBelief` â€” does not exist. TeamRuntime (SmartRuntime.cs:271-307) exposes only TeamId/Planner/Coordinator/_techs/_planTickCount/_lastPlanType.
3. **TeamBelief class has ZERO `new TeamBelief(` instantiation sites tree-wide** (grep-confirmed by 7+ reviewers). The class is orphaned with no owner.

Structural defect (consensus 6/10): Even if the three phantoms above were fixed, **Item 22 wires data into the wrong dictionary**. RoleAssignment.NeedsScout reads `TeamSnapshot.LosCoverage` built per-tick in `TeamRuntime.BuildTeamSnapshot` at SmartRuntime.cs:408,417 â€” NOT `TeamBelief._losCoverage`. The two dicts are different objects. Item 23's S1 patch is the actual fix; Item 22 is structurally redundant.

Plan self-admits: `// If SmartRuntime.Teams isn't the precise accessor name, the implementation step verifies` (REV5:717) â€” the exact "verify-at-impl-time" hedge that disqualified REV 4.

**Item 24 â€” SHIPS-AS-DESIGNED (10/10)**
Verified clean against StrategicValueFunction.cs:11-29 (WLearned=0f precedent), StrategicState.cs:11 (PositionMean exists), StrategicState.Friendly is IReadOnlyList<StrategicTechSummary>. Default-zero weight preserves byte-identical Evaluate output. Pure additive change at the documented extension surface. No phantoms. Already passed REV 4 10/10.

**Item 26 â€” BLOCKED (8/10)**
Two convergent phantoms in HealthSidecar populator (REV5:1033-1035):
1. `tank.GetComponent<ModuleDamage>()` â€” **ModuleDamage attaches to TankBlocks, not Tank**. Verified live patterns: `block.GetComponent<ModuleDamage>().maxHealth` (ManEnemyWorld.cs:2318, 2487) and `root.damage.maxHealth` where root = `tank.blockman.GetRootBlock()` (TankAIHelper.cs:3259). Tank-wide health uses `tank.visible.damageable.Health/.MaxHealth` (PlayerRTSUI.cs:394-395, on the Damageable component, NOT ModuleDamage).
2. `dmg.Health` â€” `Health` lives on Damageable, NOT ModuleDamage. TankAIHelper.cs:3259 splits the access: `root.visible.damageable.Health / (float)root.damage.maxHealth`.

Plan self-admits: `// (likely tank.dam, tank.damage, or tank.GetComponent<ModuleDamage>())` (REV5:1032) â€” explicit admission the API was never verified.

**Runtime consequence (silent feature degradation):** `GetComponent` returns null on every Tank â†’ guard `dmg != null` skips every Record call â†’ HealthSidecar stays permanently empty â†’ every tech reads HP=1.0f â†’ no candidate ever beats DamagedThreshold=0.75f â†’ RepairSupportGoalSource always falls through to Hunter. Feature is observationally indistinguishable from `EnableRepairSupport=false`. P6.1 parity-catcher would false-pass.

Rest of Item 26 (classifier hoist, b.Id/b.Team access, TacticalGoal ctor, TacticalGoal.AtCurrent fallback, BlockKindFlags.Shield, registration site) verified-correct.

**Item 27 â€” SHIPS-AS-DESIGNED with minor fix (6/10) / SHIPS-WITH-MINOR-FIXES (4/10)**
All cited APIs verified live: TacticalOptimizer.Step(BeliefState, VehicleModelSnapshot, BeliefSnapshot) at TacticalOptimizer.cs:59; IdentityContext readonly struct at SmartIdentity.cs:48-67 (6 fields, 1 ctor); BuildIdentityContext at SmartRuntime.cs:117-128 (default-null 7th param preserves 6-arg call); ContinuousController dispatch at lines 212-224; TacticalGoal.AtCurrent(pos, heading) at CostFunction.cs:28-29. Call-equivalence proof holds (same _tactical instance, same Step args, same Adam state). `else { _tactical.Step }` fallback PRESERVED â†’ addresses REV 4 S1.

Minor required fixes: (a) add `using System;` to SmartIdentity.cs for `Func<,,,>` resolution; (b) update SmartIdentity.cs:80-83 docstring (currently says "Generic intentionally NOT registered"); (c) disclosed telemetry shift â€” Generic-classified techs now log `branch=IDENTITY=Generic` instead of `branch=TACTICAL`.

---

## 3. REV 4 â†’ REV 5 Phantom-Fix Audit

| REV 4 phantom | REV 5 status |
|---|---|
| `WorldModel.GetBeliefSnapshotFor` | FIXED (Item 22 dropped this path; Item 27 uses TacticalOptimizer.Step directly) |
| `BeliefState.HpFraction` (P3-sourced) | FIXED (Item 26 now uses HealthSidecar instead of P3 cross-phase dep) |
| `BeliefState.Faction` | **PARTIALLY FIXED** â€” Item 26 correctly uses `b.Team.Equals(ownBelief.Team)`; Item 28 STILL uses `b.Faction` (REV5:1450) |
| `BeliefState.TechId` | FIXED (Item 26 uses `b.Id`) |
| `TacticalGoalKind` enum + `TacticalGoal(kind, target, heading, speed)` ctor | **PARTIALLY FIXED** â€” Item 26 uses real ctor + TacticalGoal.AtCurrent; Item 28 STILL uses `new TacticalGoal(TacticalGoalKind.Engage, ...)` (REV5:1457, 1467) |
| `TacticalOptimizer.Step` 3-arg w/ ctx | FIXED (Item 27 verified against real `(BeliefState, VehicleModelSnapshot, BeliefSnapshot)` signature) |
| `EBasePurpose.HarvestingNoHQ` | FIXED (Item 26 uses verified BasePurpose enum members) |
| `VehicleModelSnapshot.HpFraction` | FIXED (Item 26 sources HP from HealthSidecar, not VehicleModelSnapshot) |
| Existing RecordLOS loop | NEW-PHANTOM-INTRODUCED â€” Item 22 now writes to `TeamBelief._losCoverage`, which has no production consumer (RoleAssignment reads `TeamSnapshot.LosCoverage`) |
| `MonoClock.SecondsSinceStart()` | **NOT FIXED** â€” still in Item 28 (REV5:1465) |

**Net:** 7 fixed cleanly, 2 partially fixed (Item 28 left untouched with same defect class as Item 26), 1 not addressed, 1 net-new phantom introduced via Item 22's wrong-sink wiring.

---

## 4. REV 5 New Phantoms (consensus â‰¥3)

| Phantom | Cited at | Consensus | Evidence |
|---|---|:---:|---|
| `tank.GetComponent<ModuleDamage>()` | REV5:1033 (Item 26 HealthSidecar populator) | **10/10** | ModuleDamage is per-TankBlock. All live uses: `block.GetComponent<ModuleDamage>()` (ManEnemyWorld.cs:2318, 2487) or `root.damage` where root=TankBlock (TankAIHelper.cs:3259). Plan self-admits at REV5:1032. |
| `ModuleDamage.Health` (property/field) | REV5:1035 (Item 26 + P3 ReadHpFraction at REV5:440) | **5/10** | Live current-HP API is `damageable.Health` on Damageable component, not on ModuleDamage. Only `.maxHealth` is grep-verified on ModuleDamage. |
| `SmartRuntime.Teams` accessor | REV5:712 (Item 22) | **10/10** | Real API: `SmartRuntime.EnumerateTeams()` (internal static method, line 657). Plan hedges with "or however the team enumeration is exposed". |
| `TeamRuntime.TeamBelief` field/property | REV5:714 (Item 22) | **10/10** | TeamRuntime (SmartRuntime.cs:271-307) has only TeamId/Planner/Coordinator/_techs/_planTickCount/_lastPlanType. |
| **TeamBelief is never instantiated tree-wide** | REV5:714 dereferences a class with no construction site | **8/10** | Grep `new TeamBelief(` returns 0 hits in source. Class is orphaned. |
| `TacticalGoalKind.Engage / .Move` enum (Item 28) | REV5:1457, 1467 | **5/10** | No `TacticalGoalKind` enum anywhere. Real ctor: `(Vector3 pos, float heading, Vector3 vel, float lookAheadSec)` at CostFunction.cs:20. |
| `b.Faction` in Item 28 PatrolGoalSource | REV5:1450 | **5/10** | Real field is `Team` (TeamId) per BeliefState.cs:98. REV 5 fixed it in Item 26, left it broken in Item 28. |
| `MonoClock.SecondsSinceStart()` in Item 28 | REV5:1465 | **5/10** | MonoClock surface: `Now()`, `Seconds(from, to)`, `SecondsDouble(from, to)` (MonoClock.cs:30-44). No SecondsSinceStart. |
| `BeliefState?` (Nullable<sealed class>) in Item 28 | REV5:1445 | **3/10** | BeliefState is `public sealed class` (BeliefState.cs:95). Nullable<class> is CS0453 compile error in C# 7.3. Item 26 correctly uses `BeliefState candidate = null;`. |

---

## 5. REV 5 New Silent Shifts (consensus â‰¥3)

| Shift | Consensus | Evidence |
|---|:---:|---|
| **Item 22 writes to wrong dict** â€” populates `TeamBelief._losCoverage` while RoleAssignment.NeedsScout reads `TeamSnapshot.LosCoverage` (built fresh per-tick in BuildTeamSnapshot at SmartRuntime.cs:417) | **6/10** | Two different Dictionary objects. Item 22 wiring is observationally inert even if all compile blockers were fixed. |
| **Item 26 silent feature degeneration** â€” even with EnableRepairSupport=true, phantom populator â†’ empty HealthSidecar â†’ no candidate â†’ Hunter fallback. P6.1 spawn-test parity-catcher would falsely report "within tolerance" because dispatched behavior IS Hunter | **7/10** | Worse than a default-OFF demotion â€” survives the gate flip and false-confirms parity-green, masking the bug from playtesters |
| **Item 27 telemetry shift for Generic** â€” ContinuousController.cs:244-252 dispatch log flips from `branch=TACTICAL` to `branch=IDENTITY=Generic` for Generic-classified techs. P7 IdentityOutcomeConsumer now sees Generic-identity dispatches it previously didn't | **8/10** | Disclosed in plan but ungated. Distorts per-identity learning signal counts from day one. |
| **Item 26 classifier hoist subtly reroutes authored-Hunter when flag flipped** â€” Row 6 RepairSupport inserted BEFORE the `authoredHintValid && (Land || Sea) â†’ Hunter` short-circuit. Authored Hunter techs with Shield>0 reclassify as RepairSupport | **3/10** | Dormant at default-OFF; plan's "preserves Hunter classification exactly" wording is imprecise about flip-on behavior |
| **Plan internal inconsistency** â€” dependency DAG (REV5:2061-2075) and commit messages (REV5:2126-2128) still describe REV 2 demotions ("TeamBelief no-op", "GenericGoalSource DROPPED") contradicting REV 5 changelog (REV5:14-26) and Item-Accounting Table | **3/10** | A developer following commit messages would ship the demoted REV 2 shape, not the restored REV 5 shape |

---

## 6. Per-Phase Consensus Table

| Phase | WELL-FORMED | QUESTIONABLE | BROKEN | Consensus |
|---|:---:|:---:|:---:|---|
| P1 | 10 | 0 | 0 | **WELL-FORMED** |
| P2 | 10 | 0 | 0 | **WELL-FORMED** |
| P3 | 8 | 2 | 0 | **WELL-FORMED** (caveat: `ModuleDamage.Health` in ReadHpFraction unverified) |
| P4 | 10 | 0 | 0 | **WELL-FORMED** |
| P5 | 0 | 1 | 9 | **BROKEN** â€” Item 22 |
| P6 | 0 | 4 | 6 | **BROKEN** â€” Item 26 (phantom populator) + Item 28 (untouched, 5 phantoms) |
| P7 | 9 | 1 | 0 | **WELL-FORMED** (caveat: Item 27 telemetry shift ungated) |
| P8 | 10 | 0 | 0 | **WELL-FORMED** |
| P9 | 10 | 0 | 0 | **WELL-FORMED** |
| P10 | 10 | 0 | 0 | **WELL-FORMED** (gated on P5/P6 actually compiling) |

**Net:** 8 WELL-FORMED phases, 2 BROKEN phases. P5 and P6 cannot push.

---

## 7. Convergent Flaws (â‰¥3 reviewers)

| Flaw | Severity | Reviewers | Evidence | Fix |
|---|---|:---:|---|---|
| **Item 26 HealthSidecar populator uses phantom `tank.GetComponent<ModuleDamage>()`** | SEVERE | 10/10 | ModuleDamage is per-block; Tank.GetComponent returns null. Plan self-admits in comment. Silently dead at runtime. | Replace with `foreach (var block in tank.blockman.IterateBlocks()) { var dmg = block.GetComponent<ModuleDamage>(); if (dmg != null) { sumHp += dmg.Health (verify) / sumMax += dmg.maxHealth; } }` OR use `tank.visible.damageable.Health / .MaxHealth` if it aggregates at tank-level |
| **Item 22 wires three phantom APIs** (SmartRuntime.Teams, TeamRuntime.TeamBelief, never-instantiated TeamBelief) | SEVERE | 10/10 | All three grep-verified non-existent. Plan hedges twice ("verify at impl time", "wiring is mechanical"). | Either (a) drop Item 22 (Item 23's S1 patch covers the only real consumer) OR (b) add `public TeamBelief TeamBelief { get; }` to TeamRuntime + construct in ctor + rewire BuildTeamSnapshot to consume TeamBelief.LOSCoverage |
| **Item 28 (Patrol) NOT re-grounded despite carrying same phantom class as Item 26** (b.Faction, TacticalGoalKind, ctor signature, MonoClock.SecondsSinceStart, BeliefState? on sealed class) | SEVERE | 9/10 | REV 5 explicitly scoped re-grounding to Items 22/26/27; Item 28 left bit-identical to REV 4. Internal inconsistency: same plan fixes b.Faction in Item 26, leaves it broken in Item 28 200 lines later. Will fail to compile â†’ blocks P6 push. | Apply same re-grounding discipline to Item 28: `b.Team.Equals(...)`, 4-arg positional TacticalGoal ctor, `MonoClock.Now()` or `MonoClock.Seconds(...)`, `BeliefState candidate = null;` |
| **REV 5 reintroduces "verify at impl time" hedges that REV 4's process gate explicitly demanded be eliminated** | SEVERE | 7/10 | REV5:717 ("If SmartRuntime.Teams isn't the precise accessor name..."); REV5:1032 ("likely tank.dam, tank.damage, or tank.GetComponent<ModuleDamage>()"). Exact failure mode REV 4 was rejected for. | Eliminate ALL hedges in plan text; Read-verify every cited API surface before commit |
| **Item 22 wires data into wrong consumer dict** (TeamBelief._losCoverage vs TeamSnapshot.LosCoverage that RoleAssignment actually reads) | SEVERE | 6/10 | RoleAssignment.NeedsScout (RoleAssignment.cs:73-78) reads TeamSnapshot.LosCoverage built per-tick at SmartRuntime.cs:408,417 â€” NOT TeamBelief._losCoverage. Even fixing the three compile phantoms leaves Item 22 functionally inert. | Drop Item 22 (Item 23 already covers the real consumer path) OR rewrite to populate the BuildTeamSnapshot losCoverage local directly |
| **"All 43 ship materially" claim is functionally false** â€” Items 22 and 26 ship as compiled-but-no-op even with tunables ON; Item 28 won't compile | SEVERE | 9/10 | Three items have shipping code paths that either fail to compile (22 untestable, 28) or compile but silently no-op (26). Effective shipping surface: ~38-40/43, not 43/43. | Either honestly demote 22/26/28 in plan text, or fix them so they materially function |
| **Item 27 P7 IdentityOutcome telemetry distortion is ungated** | MEDIUM | 4/10 | Per-identity learning buckets see Generic traffic from day one (previously invisible). REV5:1385 acknowledges but no default-OFF gate or filter | Add default-OFF gate on Generic-counting in P7 IdentityOutcomeConsumer, OR explicitly accept symmetry with comment |
| **Plan internal inconsistency** â€” DAG (REV5:2061-2075) and commit messages (REV5:2126-2128) still describe REV 2 demotions, contradicting REV 5 restorations | MEDIUM | 3/10 | A developer following commit messages would ship the demoted REV 2 shape. | Refresh DAG and commit-message sections to match REV 5 changelog and Item-Accounting Table |

---

## 8. Notable Singletons

- **Item 26 Shield-bit conflation** (1 reviewer): RepairSupport classifies on `KindCounts.Shield > 0`, but ModuleShieldGenerator is energy absorption, not heal-beam. REV 3 explicitly renamed IsRepairâ†’HasProtection to fix this conflation; REV 5's classifier re-introduces it. A pure-shield escort tech (no heal capability) gets RepairSupport and orbits damaged allies who never receive healing. Behavior is degenerate but not crashy.
- **Item 22 cross-thread safety** (2 reviewers): TeamBelief._losCoverage is a plain Dictionary, not ConcurrentDictionary. Item 22 wiring is main-thread; any future consumer on Coordinator worker thread would race. Moot today since no consumer.
- **Item 26 HealthSidecar lifecycle gap** (1 reviewer): Forget hooks are on SmartRuntime.Deregister and WorldModel.DeregisterTech (Smart-driven techs only). Observer iterates ALL ManTechs (friendly + enemy + neutral); enemy techs never deregistered through these hooks would leak entries over long sessions.
- **Item 27 cosmetic comment incoherence in Item 24** (1 reviewer): REV5:853 inline comment says "mean cos-similarity" while math computes 1âˆ’R (mean resultant length). Math correct, comment wrong. Flagged in REV 4, untouched in REV 5.
- **Item 22 Coordination namespace prefix redundancy** (1 reviewer): TeamBelief.cs is already in `TAC_AI.AI.Forms.Smart.Coordination`; the `Coordination.LineOfSightProducer.Enabled` prefix at REV5:696 is either redundant or refers to a phantom sub-namespace.
- **Item 26 RepairSupport hot-path perf claim unverified** (2 reviewers): Plan claims "~50ns per tech per 33ms tick" for HealthSidecar populator. Unity GetComponent<T>() is ~200-500ns; per-block iteration multiplies by N_blocks. Real cost is O(blocks_per_tank), not O(1). Cost claim assumes the accessor works (it doesn't).
- **GetComponent allocation pressure** (1 reviewer): Per-tech-per-33ms GetComponent across 80 techs at 30Hz = 2400 calls/sec, measurable pressure even if accessor were correct.

---

## 9. No-Deferment Verification

**Claim:** "All 43 items materially ship." (REV5:14-26 changelog + REV5:83-94 Item-Accounting Table)

**Verdict: FALSE (10/10 consensus).**

| Item | Status | Effective shipping |
|---|---|---|
| 22 | BLOCKED â€” 3 compile phantoms + wrong-sink wiring | Compiled-but-no-op (or won't compile at all) |
| 24 | SHIPS clean | Materially ships |
| 26 | BLOCKED â€” phantom populator | Compiles but silently degenerates to Hunter; observationally indistinguishable from feature-OFF |
| 27 | SHIPS with minor fix | Materially ships once `using System;` added |
| 28 | NOT re-grounded â€” 5 carryover phantoms | Won't compile â†’ blocks P6 push |

**Effective shipping surface: ~38-40 of 43.** REV 5 hid demotions inside phantom APIs and dead-end wiring rather than fixing them. The headline is technically true at the file-and-registration level (every item has SOME code) but materially false (Items 22, 26 produce no observable behavior; Item 28 fails to compile).

---

## 10. Ship Verdict

**NEEDS-REVISION (REV 6 required). 10/10 reviewer consensus.**

### Trajectory

| Revision | Outcome | Notes |
|---|---|---|
| REV 1 | 10/10 NEEDS-REVISION | Phantom-API cascade |
| REV 2 | 5/5 SHIP-WITH-MINOR-FIXES | Wholesale corrections + demotions (Items 22/26/27 demoted to no-ops) |
| REV 4 (no-deferment attempt) | 0/10 SHIP | Restored items reintroduced phantoms |
| **REV 5** | **0/10 SHIP** | Item 27 genuinely fixed + Item 24 still clean; Items 22, 26 reintroduce same phantom pattern that disqualified REV 4; Item 28 left entirely untouched with same defect class |

REV 5 made real progress (Item 27 is now the first genuinely sound restoration of the no-deferment effort), but the discipline failure REV 4 was rejected for has not been corrected. The "verify at impl time" hedge appears verbatim in two places (REV5:717, REV5:1032). Three of four flagship restorations are still in scope-creep territory.

---

## 11. If SHIP-WITH-MINOR-FIXES â€” concrete patch list (for REV 6)

Reviewers were unanimous: REV 5 cannot ship as-is, but the gap to a shippable REV 6 is well-bounded. Required patches:

### P-REV6-1 (SEVERE): Fix Item 26 HealthSidecar populator
Replace REV5:1031-1035:
```csharp
// Phantom:
var dmg = tank.GetComponent<ModuleDamage>();
if (dmg != null && dmg.maxHealth > 0) {
    SmartRuntime.Health.Record(techId, dmg.Health / (float)dmg.maxHealth);
}
```
With one of:
- **Option A (per-block aggregate, accurate):** iterate `tank.blockman.IterateBlocks()` summing per-block `block.GetComponent<ModuleDamage>().Health` and `.maxHealth`. Honestly disclose O(blocks_per_tech) cost.
- **Option B (Damageable, simpler):** `var dam = tank.visible?.damageable; if (dam != null && dam.MaxHealth > 0f) Record(techId, dam.Health / dam.MaxHealth);` â€” verify Damageable.Health/MaxHealth surface at PlayerRTSUI.cs:394-395.

### P-REV6-2 (SEVERE): Fix or drop Item 22
- **Option A (drop):** Item 23's S1 patch at SmartRuntime.cs:417 already covers the only real consumer (RoleAssignment.NeedsScout via TeamSnapshot.LosCoverage). Item 22 adds zero value. Drop and re-attribute the LOC budget.
- **Option B (actually wire):** Add `public TeamBelief TeamBelief { get; }` property to TeamRuntime (SmartRuntime.cs:271-307); construct `new TeamBelief(SmartRuntime.World)` in TeamRuntime ctor; rewrite BuildTeamSnapshot at SmartRuntime.cs:408-417 to consume TeamBelief.LOSCoverage instead of building a fresh local. Fix `SmartRuntime.Teams` â†’ `SmartRuntime.EnumerateTeams()`.

### P-REV6-3 (SEVERE): Re-ground Item 28 (Patrol)
Apply same discipline as Item 26's REV 5 rewrite:
- `b.Faction == ownBelief.Faction` â†’ `b.Team.Equals(ownBelief.Team)` (REV5:1450)
- `new TacticalGoal(TacticalGoalKind.Engage, target:, heading:, speed:)` â†’ real ctor `new TacticalGoal(Vector3 pos, float heading, Vector3 vel, float lookAheadSec)` (REV5:1457, 1467)
- `BeliefState? nearest = null;` â†’ `BeliefState nearest = null;` (REV5:1445; sealed class can't be Nullable<T>)
- `MonoClock.SecondsSinceStart()` â†’ `MonoClock.Seconds(0L, MonoClock.Now())` or equivalent (REV5:1465)
- `heading: offset.normalized` â†’ `heading: angleFloat` (TacticalGoal.Heading is float, not Vector3)

### P-REV6-4 (SEVERE): Eliminate all "verify at impl time" hedges
- REV5:717 `"If SmartRuntime.Teams isn't the precise accessor name..."` â†’ name the real API (`SmartRuntime.EnumerateTeams()`)
- REV5:1032 `"// (likely tank.dam, tank.damage, or tank.GetComponent<ModuleDamage>())"` â†’ name the verified accessor chosen for P-REV6-1
- Audit plan tree-wide for any other hedge phrases; eliminate before commit

### P-REV6-5 (MEDIUM): Refresh stale plan artifacts
- Dependency DAG (REV5:2061-2075) â€” remove REV 2 demotion language ("TeamBelief no-op", "GenericGoalSource DROPPED")
- Commit messages (REV5:2126-2128) â€” match REV 5 restoration shape
- Phase 6 cross-phase claim (REV5:1502) â€” remove stale "Item 26 reads BeliefState.HpFraction, added in P3" since HpFraction dependency was dropped for HealthSidecar
- Phase 6 Risk Assessment (REV5:1526-1539) â€” refresh to match restorations

### P-REV6-6 (MEDIUM): Item 27 housekeeping
- Add `using System;` to SmartIdentity.cs:1 (currently only `using System.Collections.Generic;`)
- Update SmartIdentity.cs:80-83 docstring (currently says Generic intentionally NOT registered)
- Decide on P7 IdentityOutcomeConsumer Generic-routing policy: default-OFF gate OR explicit comment accepting symmetry

### P-REV6-7 (MINOR): Item 22 changelog/body contradiction
REV5:18 vs REV5:723-731 â€” changelog says Enabled=false â†’ "RecordLOS clears the map"; body says clear is gated on Enabled=true. Pick one and rewrite both consistently. (Moot if Item 22 dropped per P-REV6-2-A.)

### P-REV6-8 (MINOR): Item 26 RepairSupport-vs-Shield semantic conflation
Reconsider classifier predicate `armed && mobile && ground && KindCounts.Shield > 0` â€” Shield bit fires on ModuleShieldGenerator (energy absorption), not heal-beam. Either rename identity to ShieldEscort, or change predicate to `KindCounts.HealBeam > 0` (verify BlockKindFlags has HealBeam first).

**With P-REV6-1 through P-REV6-4 applied, REV 6 should reach 9-10/10 SHIP-WITH-MINOR-FIXES.** Items 24 and 27 are already there; the bottleneck is the three discipline failures in Items 22, 26, 28.
