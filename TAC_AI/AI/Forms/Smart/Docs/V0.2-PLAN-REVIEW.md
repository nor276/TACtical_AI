# V0.2-PLAN Adversarial Review Reconciliation

## 1. Verdict Tally
- **NEEDS-REVISION:** 10/10
- WELL-FORMED / READY: 0
- FULL-REWRITE: 0

**Unanimous: revision required.**

## 2. Item Accounting Consensus
All 10 reviewers report **43/43 placed, zero duplicates, zero omissions**. No accounting disagreement. (Two reviewers flagged items 13 and 27 as semantically questionable but placement-correct.)

## 3. Per-Phase Consensus Table

| Phase | WELL-FORMED | QUESTIONABLE | BROKEN |
|------:|:-----------:|:------------:|:------:|
| P1    | 1 | 9 | 0 |
| P2    | 2 | 6 | 2 |
| P3    | 3 | 3 | 4 |
| P4    | 0 | 1 | 9 |
| P5    | 1 | 3 | 6 |
| P6    | 1 | 4 | 5 |
| P7    | 0 | 8 | 2 |
| P8    | 6 | 4 | 0 |
| P9    | 0 | 6 | 4 |
| P10   | 2 | 8 | 0 |

**Convergence: P4 + P5 + P6 are universally rated broken/questionable. P8 is the cleanest phase. P3 is contested.**

## 4. Convergent Flaws (â‰¥3 reviewers)

| # | Severity | Where | Evidence | Reviewers | Fix |
|---|:--------:|-------|----------|:---------:|-----|
| **C1** | SEVERE | **P4 phantom citation** â€” `SmartEventBridge.cs:244` cited as DamageObserved producer | Line 244 is `IdentityOutcome(KillScored)` publish; real `DamageObserved` producer is at **line 320** (`OnTankDamage`) | 10/10 | Re-cite line 320 |
| **C2** | SEVERE | **P4 silent semantic shift** â€” `PositionAt/VelocityAt` swap in `WeaponFireController.cs:177-180` claimed "bit-identical at Sight=Fresh" | `BeliefState.cs:184` only returns `VelocityMean` when `dt<=0`; Control ticks always have `dt>0` post-publish, so `DeadReckon.ExtrapolateVelocity` (~exp decay) is returned. Lead math silently shifts. Same failure class as Aether REV 1. | 10/10 | Gate on `UseCoastAwareLead=false` default, or fix invariant claim |
| **C3** | SEVERE | **P9 phantom citation** â€” `MaxWorkers=22` claimed to match `WorkerPool.cs:189` "observed cap" | Line 189 is a code COMMENT inside `ThreadAbortException` catch describing one incident on a 24-core box. `WorkerPool.cs:43` explicitly states "No hard upper cap (FORM-SPEC Â§5.4)". 22-cap is fabricated. | 9/10 | Remove cap or replace with real spec value |
| **C4** | SEVERE | **P10 spawn-test scope under-stated** â€” "P10 is the ONLY phase needing spawn-test" | P1 (ForwardDirectionLocal swap shifts arc decisions for canted barrels), P3 (HP ranking shift), P4 (lead math), P5 (Scout fraction / role assignment), P6 (Patrol steals from Hunter; classifier reorder) all silently change combat behavior | 10/10 | Per-phase tunable gates default-OFF, OR explicit playtest gates after P1/P4/P6 |
| **C5** | SEVERE | **P5 architectural mis-target** â€” `PerimeterCoverageCost.COverlap` inserted in `PlanDecomposition.DecomposeDefensivePerimeter` is described as "PUCT plan ranking cost" | `PlanDecomposition` decomposes already-selected plans into per-tech goals; PUCT ranking lives in `StrategicPlanner`/`PUCTSearch`. Cost never enters the ranker. | 8/10 | Move insertion to `StrategicPlanner`/`PUCTSearch` |
| **C6** | SEVERE | **P5 threading violation** â€” `LineOfSightProducer.MainThreadTick` (uses `Physics.Raycast`) invoked from `Coordinator.TickOnce` | `Coordinator.TickOnce` runs on `GlobalCoordinatorDaemon` worker (`SmartRuntime.cs:548`). Unity Physics is main-thread-only. Method literally named `MainThreadTick`. | 7/10 | Remove worker-side call; main-thread tick only + worker reads via snapshot buffer |
| **C7** | SEVERE | **P6 GenericGoalSource singleton â†” per-controller state** â€” claim "delegates to TacticalOptimizer.Step, semantically identical" | `ContinuousController._tactical` is per-tech instance with `Reset()` at line 209, Adam state. Singleton `ISmartGoalSource` has no per-tech context plumbing. | 7/10 | Either re-thread per-tech context, or drop the dispatch refactor, or preserve the Generic bypass |
| **C8** | SEVERE | **P6 inverts documented design** â€” removes `if (src.Identity != Generic)` shortcut at `ContinuousController.cs:213` | `SmartRuntime.cs:498-501` + `SMART-IDENTITY-DESIGN.md sec 7.3` explicitly document "Generic intentionally NOT registered". Plan reverses this without updating the design doc (P10 docs work doesn't list it). | 5/10 | Update design doc OR keep the bypass |
| **C9** | SEVERE | **P6 shape error** â€” classifier rules reference `archetype.Kinds & BlockKindFlags.Drill` | `SmartIdentityClassifier.Classify` receives `VehicleModelSnapshot vehicle`, NOT an archetype. Correct access is `vehicle.KindCounts.Drill > 0` (per `BlockKindCounts.cs`). | 5/10 | Rewrite as `vehicle.KindCounts.Drill > 0` |
| **C10** | SEVERE | **P7 cross-class plumbing gap** â€” PlanTransitionPublisher wired into `Coordinator.TickOnce` referencing `_lastPlanType` "tracked at SmartRuntime.cs:381" | `_lastPlanType` is private field on `TeamRuntime`, a different class on a different worker. `Coordinator` has no access. Compiles green, silently no-ops. | 7/10 | Add tracking in Coordinator or expose accessor |
| **C11** | SEVERE | **P4 DamageHintBuffer direction is dead** â€” `DirectionWorld` co-publish from `SanitizedDamageInfo` | `SmartEventBridge.cs:307-318` documents engine `ManDamage.DamageInfo` has no `HitPosition`/`HitForceDir`; sanitized direction is hardwired `Vector3.zero`. P6 RepairSupport's damaged-ally orbit consumer is non-functional. | 5/10 | Disclose limitation; drop direction channel from RepairSupport design until reflection solved |
| **C12** | SEVERE | **P9 C# const-vs-default-parameter compile bug** â€” `CircuitBreaker.DefaultMaxFailures`/`DefaultWindowMs` are `public const int` AND used as default param values at ctor line 34 | Default parameter values require compile-time constants. Switching to `AetherTuning` static fields breaks ctor signature OR forces ABI break (const inlines, static-readonly doesn't). | 4/10 | Keep consts; read AetherTuning inside ctor body |
| **C13** | MEDIUM | **P1 silent ForwardDirectionLocal swap** â€” `WeaponProfile.cs:98` swap `pose.LocalRotation * Vector3.forward` â†’ `* spec.BarrelDirBlockLocal` | Geometrically identical only for forward-facing barrels. Side-mount/canted turrets WILL get different `IsAimedWithinArc` (lines 318-361) decisions on first push. | 5/10 | Gate on `UseHonestBarrelGeometry=false` default or playtest |
| **C14** | MEDIUM | **P1 UseReflectedScalars=true default** â€” every weapon range/damage/projVel/arc shifts from PlaceholderFixed on first push | Reflection-success path is the dominant path, not the fallback. Combat feel diverges immediately. | 4/10 | Default OFF, flip after parity-log green |
| **C15** | MEDIUM | **TripleBuffer<T> where T : class regresses DoubleBuffer design** | `DoubleBuffer.cs:15-20` comment explicitly removed the `class` constraint to support readonly-struct snapshots (FIX-PLAN Phase 1.2). P4's `TripleBuffer<T> where T : class` reintroduces the dropped bug. | 4/10 | Drop constraint to match sibling |
| **C16** | MEDIUM | **Citation drift** â€” multiple cites off by 1-60 lines | `RWeapSetup.cs:91-104` (actual GimbalAimer at 52-66); `WeaponProfile.cs:96-104` (actual swap at line 98); `csproj:512-533` (actual entries start ~452); `OutcomeKind` enum at 100-106 not 108-122 | 6/10 | Re-verify line ranges before push |

## 5. Notable Singletons (1-2 reviewers)

| Finding | Reviewer(s) | Credibility |
|---------|:-----------:|-------------|
| **P2 ModuleDrill/ModuleHealBeam/ModuleGyro are phantom engine types** (zero codebase refs; BlockKindFlags.cs comment explicitly warns) | 2 | **HIGH â€” sourced, grep-confirmed.** If true, cascades to P6 (RepairSupport never fires, Gatherer Drill augment dead). |
| **P3 `m_StartHealth` is phantom field** (zero matches; verified API is public `.maxHealth` at ManEnemyWorld.cs:2318) | 1 | **HIGH â€” grep-confirmed.** If true, P3 SpecHP silently no-ops. |
| **P3 fallback to `archetype.specMass` â‰  v0.1 `pose.CurrentMass`** (instance-vs-prefab divergence on damaged blocks) | 2 | **HIGH â€” sourced.** Breaks "bit-identical" claim. |
| **P2 ThrustEmitter readonly-struct ctor break** (adding `FanSpinThreshold` field breaks 5+ call sites; C# 7.3 has no struct field initializers) | 2 | **HIGH â€” sourced.** |
| **P5 `TeamBelief.InjectObservations(Tank)` is engine-type / main-thread only** (threading risk if called from coordinator) | 2 | **MEDIUM â€” speculative on thread but real concern.** |
| **P7 PlayerIdResolver field names speculation** (`LearningService.cs:36-43` documents prior search failed) | 4 | **HIGH â€” sourced.** Plan needs to acknowledge it's still deferred. |
| **P9 PathRequest.Priority readonly-struct addition** (field default = 0, "default 128 via overload" is incoherent for struct fields) | 3 | **HIGH â€” sourced.** |
| **P5 Observer.Submit single-writer contract** (Observer.cs:25-26) violated if both engine observer and LOS bridge write to same intake | 1 | **MEDIUM â€” sourced but unclear if interleaving is actually unsafe.** |
| **P10 csproj timing inconsistency** â€” plan says P10 adds all P1-P9 csproj entries AND each phase ships green with its own entries | 2 | **HIGH â€” internal contradiction.** Must pick. |
| **P6 Patrol classifier "no-nearby-hostile" needs BeliefSnapshot but Classify signature doesn't take one** | 4 | **HIGH â€” sourced.** |
| **P8 schema bump triggers migration default-throw without `case 1:`** | 1 | **HIGH â€” sourced (ProfilePersistence.cs:299-317).** Plan does add the case but understates impact. |
| **`AetherTunables` vs `AetherTuning`** class-name error in P4 sketch | 1 | **HIGH â€” sourced.** Minor typo, real. |

## 6. Factual Errors (objective, live-code contradicted)

| # | Claim | Reality | Source |
|---|-------|---------|--------|
| F1 | `SmartEventBridge.cs:244` = DamageObserved producer | Line 244 is `IdentityOutcome(KillScored)` publish; real `DamageObserved` at line 320 | `SmartEventBridge.cs:244,320` (10/10 confirm) |
| F2 | `VelocityAt/PositionAt` swap "bit-identical at Sight=Fresh" | `BeliefState.cs:184` returns `VelocityMean` only when `dt<=0`; Control-tick dt is positive in production | `BeliefState.cs:180-186` + `DeadReckon.cs:62-74` (9/10) |
| F3 | `WorkerPool.cs:189` "22-worker observed cap" | Line 189 is comment inside `ThreadAbortException` catch about one incident; `WorkerPool.cs:43` says "No hard upper cap" | `WorkerPool.cs:43,189` (9/10) |
| F4 | `RWeapSetup.cs:91-104` = GimbalAimer rotation-range pattern | Lines 91-105 are missile/laser/shotgun branch; GimbalAimer at lines 52-66 (`CircleRange` const) | `RWeapSetup.cs:52-105` (5/10) |
| F5 | `WeaponProfileBuilder.cs:96-104` swap site | No such file; type lives in `WeaponProfile.cs`, line 98 | Glob (3/10) |
| F6 | `PlanDecomposition` is the PUCT plan-ranking cost insertion site | Plan ranking happens in `StrategicPlanner`/`PUCTSearch`; PlanDecomposition expands already-selected plans | `PlanDecomposition.cs:160-181` + `StrategicPlanner.cs:14` (4/10) |
| F7 | `ModuleDrill`/`ModuleHealBeam`/`ModuleGyro` exist as engine types | Zero grep matches; `BlockKindFlags.cs:13-16` design comment explicitly warns | Grep (2/10) |
| F8 | `m_StartHealth` is a ModuleDamage field | Zero matches; verified API is public `.maxHealth` | Grep + `ManEnemyWorld.cs:2318` (1/10) |
| F9 | P3 SpecHP=specMass fallback equals v0.1 mass-as-HP distribution | v0.1 uses `b.CurrentMass` per-pose (live); plan uses archetype prefab `specMass`. Damaged-block distribution shifts. | `ArmorMap.cs:172` + `ChassisCapture.cs:52` + `BlockInstancePose.cs:20` (2/10) |
| F10 | DamageHint `DirectionWorld` is usable | `SanitizedDamageInfo.ImpactDirectionWorld` is hardwired `Vector3.zero` (no engine accessor) | `SmartEventBridge.cs:307-318` (5/10) |
| F11 | `_lastPlanType` accessible from Coordinator | Private field on `TeamRuntime`, different class on different worker | `SmartRuntime.cs:283,381` vs `Coordinator.cs:152-160` (5/10) |
| F12 | EventBus `OutcomeKind` enum at lines 108-122 | Enum at 100-106; struct at 108-122 | `EventBus.cs:100-122` (2/10) |
| F13 | `AetherTunables.VelocitySmoothingStrength` | Class is `AetherTuning` | `AetherTuning.cs:15` (1/10) |
| F14 | `ClassifyKind` uses 170Â° rotation-range gate | Live code (`RWeapSetup.cs:89-91`) gates on `m_RotateSpeed > MinCircleSpeed`, not 170Â° | `RWeapSetup.cs:89-91` (1/10) |

## 7. Item-Placement Disputes
**No reviewer claims any item is in the wrong phase.** Two soft flags:
- **Item 13 (TripleBuffer<T>):** placed in P4, zero consumers until P9 (may never be wired). Acceptable forward-staging or dead-code-on-arrival. (3 reviewers noted.)
- **Item 27 (Generic GoalSource):** placed in P6 but inverts documented design (see C8). Two reviewers flagged it shouldn't exist in this form without doc rewrite.

No structural reassignments demanded.

## 8. Ship Verdict

**NEEDS FULL REVISION before P1 start.**

Unanimous NEEDS-REVISION across 10/10 reviewers, with **convergent severe findings on the same Aether REV 1 / Chassis REV 1 failure modes that previously rejected those plans:**

1. **Phantom citations** (F1, F3 unanimous; F4, F5, F6, F11 multi-reviewer)
2. **Silent semantic shifts unguarded by tunable flags** â€” P4 lead math (10/10), P1 turret arc (5/10), P6 Patrol classifier reroute (5/10), P3 ArmorMap ranking (2/10)
3. **Shape/threading errors that won't compile or will race** â€” P5 worker-thread `Physics.Raycast` (7/10), P6 `archetype.Kinds` vs `vehicle.KindCounts` (5/10), P9 const-vs-default-parameter (4/10), P6 GenericGoalSource per-tech state (7/10)
4. **Cross-class plumbing gaps** â€” P7 PlanTransition `_lastPlanType` (5/10)
5. **Spawn-test scope dishonest** â€” "only P10" claim unsupportable (10/10)

A REV 2 lint pass is **insufficient** â€” P4, P5, P6 each need structural rework (not just citation fixes). P3, P7, P9 need targeted fixes. P1 needs tunable gates. Only P8, P10 (tooling), and arguably P2 (excluding the phantom-module question) are close to push-ready.

**Recommended next step:** revise plan with (a) every behavior-changing swap gated on a default-OFF tunable, (b) all phantom citations corrected against live source, (c) P5 redesigned with main-thread-only LOS production + worker-readable snapshot, (d) P6 GenericGoalSource design resolved or dispatch refactor dropped, (e) P10 spawn-test gates added after P1/P4/P6 minimum, (f) item 27's design-doc inversion either documented or reverted. Re-run adversarial fan-out before P1 commits.
