# V0.2-PLAN-REV4 Adversarial Review Reconciliation

## 1. Verdict Tally

| Verdict | Count |
|---|---|
| FATAL-FLAW-BLOCK-PLAN | 5 |
| NEEDS-REVISION | 5 |
| SHIP / SHIP-WITH-MINOR-FIXES | 0 |

**Headline: 0/10 ship. 5/10 fatal, 5/10 needs-revision. Unanimous rejection of REV 4 as written.**

The split between FATAL and NEEDS-REVISION is essentially cosmetic â€” all 10 reviewers identify the same 3 broken restorations against the same phantom APIs. The NEEDS-REVISION verdicts simply frame the fix path as "rewrite 3 items in REV 5" rather than "block the plan." Substantively unanimous.

---

## 2. Restoration Audit (MOST IMPORTANT)

| Item | SHIPS-AS-DESIGNED | SHIPS-WITH-MINOR-FIXES | BLOCKED | DEMOTED-AGAIN | Consensus |
|---|---|---|---|---|---|
| **Item 22** (TeamBelief shared belief) | 0 | 0 | **10** | 0 | BLOCKED (unanimous) |
| **Item 24** (Perimeter coverage) | **9** | **1** | 0 | 0 | SHIPS (unanimous, 1 cosmetic nit) |
| **Item 26** (RepairSupport) | 0 | 0 | **10** | 0 | BLOCKED (unanimous) |
| **Item 27** (GenericGoalSource via handle) | 0 | 0 | **10** | 0 | BLOCKED (unanimous) |

### Evidence summary per item

**Item 22 â€” BLOCKED (10/10):** Depends on `WorldModel.GetBeliefSnapshotFor(TechId)` which does not exist. Real accessors are `GetPerTechBuffer(TechId) -> DoubleBuffer<BeliefState>` (single belief, no ByTech) and `FusedBuffer -> DoubleBuffer<BeliefSnapshot>` (one team-wide snapshot). The plan's own footnote "verify at impl time; if private, expose via getter" admits non-verification. Several reviewers note the concept is architecturally absent â€” there is no per-friendly belief-snapshot stored anywhere; per-friendly iteration over `snap.ByTech` would either fail to compile or, if backed by FusedBuffer, would union identical data N times (degenerate). Wiring claim "alongside the existing RecordLOS loop" is also phantom â€” `RecordLOS` has zero callers in the codebase. Cadence claim "per-team tick" mismatches reality (`SmartForm.Operations` is per-tech-per-frame).

**Item 24 â€” SHIPS (10/10):** `StrategicState.Friendly` is `IReadOnlyList<StrategicTechSummary>` (StrategicState.cs:34), `StrategicTechSummary.PositionMean` exists (line 11), `StrategicValueFunction` has the `WLearned=0f` precedent at line 18. Angular-dispersion math (R = sqrt(sumCosÂ² + sumSinÂ²)/N; coverage = 1 - R) is the standard circular-statistics mean resultant length. Default-zero weight preserves byte-identity. One reviewer flagged a minor documentation incoherence in the inline comment ("mean cos-similarity" wording is wrong though math is correct) and one noted XZ projection silently flattens aircraft altitude â€” neither blocks shipping.

**Item 26 â€” BLOCKED (10/10):** Six independent compile-blockers consistently identified:
1. `b.HpFraction` â€” BeliefState has no HpFraction field; P3 only adds HpFraction to BlockInstancePose (per-block), not BeliefState. Cross-phase dependency is fabricated.
2. `b.Faction` â€” real field is `Team` (TeamId).
3. `b.TechId` â€” real field is `Id` (TechId-typed).
4. `TacticalGoalKind` enum â€” doesn't exist anywhere in codebase; real ctor is `TacticalGoal(Vector3 pos, float heading, Vector3 vel, float lookAheadSec)` per CostFunction.cs:20.
5. `EBasePurpose.HarvestingNoHQ` â€” real enum is `BasePurpose` (the file is named EBasePurpose.cs but the type inside is BasePurpose).
6. Classifier inserts row BEFORE Hunter row but references `armed/mobile/aircraftLike` locals declared INSIDE the Hunter row (SmartIdentityClassifier.cs:126-128).

Also: `purposes` may be null (SmartIdentityClassifier.cs:35); plan's `purposes.Contains(...)` NREs unauthored techs.

**Item 27 â€” BLOCKED (10/10):** Delegate type `Func<BeliefState, BeliefSnapshot, IdentityContext, TacticalGoal>` mismatches the real `TacticalOptimizer.Step(BeliefState, VehicleModelSnapshot, BeliefSnapshot)` signature (TacticalOptimizer.cs:59). The middle arg is VehicleModelSnapshot, not BeliefSnapshot; there is no IdentityContext param. The closure `(belief, snap, ctx) => _tactical.Step(belief, snap, ctx)` cannot type-check. Additionally, BuildIdentityContext lives on `SmartPerTechState` (SmartRuntime.cs:117), not on `ContinuousController` â€” the wiring path between handle-owner and context-builder is unspecified.

---

## 3. REV 4 New Phantoms (consensus â‰¥3)

| Phantom | whereCited | Evidence | Reviewers |
|---|---|---|---|
| `WorldModel.GetBeliefSnapshotFor(TechId)` | REV4:683, 715 | WorldModel.cs has only `GetPerTechBuffer(TechId)->DoubleBuffer<BeliefState>` and `FusedBuffer->DoubleBuffer<BeliefSnapshot>`. No per-tech snapshot accessor. Plan self-hedges. | 10/10 |
| `BeliefState.HpFraction` | REV4:1022, 1049, 1058, 1318 | BeliefState.cs:95-160 has no HpFraction. P3 adds it to BlockInstancePose only. Cross-phase dependency fabricated. | 10/10 |
| `BeliefState.Faction` | REV4:1017, 1265 | Real field is `Team` (TeamId) per BeliefState.cs:98. HunterGoalSource.cs:59 uses `pair.Value.Team.Equals(ownTeam)`. | 10/10 |
| `BeliefState.TechId` | REV4:1018 | Real field is `Id` (TechId-typed) per BeliefState.cs:97. | 10/10 |
| `TacticalGoalKind` enum + `(kind, target, heading, speed)` ctor | REV4:1031, 1272, 1282 | TacticalGoal ctor is `(Vector3 pos, float heading, Vector3 vel, float lookAheadSec)` per CostFunction.cs:20. No Kind enum exists anywhere in codebase. | 10/10 |
| `TacticalOptimizer.Step(BeliefState, BeliefSnapshot, IdentityContext)` signature | REV4:1099, 1123, 1146 | Real signature is `(BeliefState, VehicleModelSnapshot, BeliefSnapshot)` per TacticalOptimizer.cs:59. | 10/10 |
| `EBasePurpose.HarvestingNoHQ` (wrong type name) | REV4:989 | Real type is `BasePurpose` per EBasePurpose.cs:30 + SmartIdentityClassifier.cs:42-77. | 10/10 |
| `VehicleModelSnapshot.HpFraction` | REV4:31 | VehicleModelSnapshot has TechIdValue/Mass/Thrust/Weapons/Armor/Kinematics/Mobility/KindCounts. No HpFraction. P3 doesn't add it. | 4/10 |
| "existing RecordLOS loop" call site | REV4:705 | TeamBelief.cs:32 is the only RecordLOS reference; zero callers. | 4/10 |
| `MonoClock.SecondsSinceStart()` | REV4:1280 (Item 28) | MonoClock surface is `Now()`, `Seconds(from, to)`, `SecondsDouble(from, to)`. No SecondsSinceStart. | 3/10 |

---

## 4. REV 4 New Silent Shifts (consensus â‰¥3)

| Shift | Evidence | Reviewers |
|---|---|---|
| **Item 27 dispatch deletion of `_tactical.Step` fallback** introduces `default(TacticalGoal)` (zero Position) failure mode | REV4:1163 â€” `goal = src?.Produce(...) ?? default;` deletes the `else { goal = _tactical.Step(...); }` branch. If GenericGoalSource is unregistered (race/init order/test harness), tech drives to world origin. NaN-guard at ContinuousController.cs:229 doesn't catch zero. | 7/10 |
| **Item 22 cadence misalignment** â€” "per-team tick" doesn't exist on main thread | `SmartForm.Operations` is per-tech-per-frame, not per-team. Without an Interlocked guard like ObserveWorldTechsIfDue, RefreshSharedBeliefs would fire N times per team per frame. | 6/10 |
| **Item 22 SPSC claim toothless without compilable producer + real consumer wiring** | Plan asserts "consumers light up opportunistically" but lists no specific RoleAssignment/Hunter/Sniper/Patrol patch. Even if producer worked, consumers wouldn't read it. Plus Dictionary read concurrency deferred to v0.3 â€” itself a deferment. | 5/10 |
| **Item 27 P7 IdentityOutcome routing change has no default-OFF gate** | Plan line 1200: Generic now visible in IdentityOutcomeConsumer telemetry. Distorts P7 learning signals from day one. | 3/10 |

---

## 5. No-Deferment Verification Consensus

**Unanimous: 0/10 reviewers confirm "all 43 ship materially."**

All 10 reviewers reach the same arithmetic:
- **Item 24** = 1 real restoration
- **Items 22, 26, 27** = 3 paper restorations that don't compile

Several frame this as REGRESSION from REV 3: REV 3 had honest demotions documented in its honesty table. REV 4 substituted no-deferment **performance** for no-deferment **delivery** â€” burying the same blockers behind fictional APIs and self-contradicting cross-phase claims (most egregiously P3 supposedly adding `BeliefState.HpFraction` when P3's own section only adds it to `BlockInstancePose`).

**Effective shipping surface**: ~40/43 (Items 22/26/27 still effectively demoted; Item 24 newly added).

---

## 6. Per-Phase Consensus Table

| Phase | WELL-FORMED | QUESTIONABLE | BROKEN | Verdict |
|---|---|---|---|---|
| P1 | 10 | 0 | 0 | WELL-FORMED |
| P2 | 9 | 1 | 0 | WELL-FORMED |
| P3 | 6 | 4 | 0 | QUESTIONABLE (cross-phase phantom HpFraction-on-BeliefState claim) |
| P4 | 10 | 0 | 0 | WELL-FORMED |
| **P5** | **0** | **0** | **10** | **BROKEN (Item 22)** |
| **P6** | **0** | **0** | **10** | **BROKEN (Items 26 + 27)** |
| P7 | 8 | 2 | 0 | WELL-FORMED (with Item 27 P7 telemetry shift caveat) |
| P8 | 10 | 0 | 0 | WELL-FORMED |
| P9 | 9 | 1 | 0 | WELL-FORMED |
| P10 | 7 | 3 | 0 | WELL-FORMED (gated on P5/P6 actually compiling) |

**Trajectory note**: REV 3 had P1/P3/P8 unanimous WELL-FORMED. **REV 4 preserves P1/P8 but downgrades P3 to QUESTIONABLE** (cross-phase phantom in Item 26 references a P3 deliverable P3 doesn't promise). Worse, REV 4 **breaks P5 and P6** that REV 3 had not damaged (REV 3's P5/P6 were honest about demotions). Net: REV 4 traded P5/P6 honesty for P5/P6 brokenness, plus muddied P3.

---

## 7. Convergent Flaws (â‰¥3 reviewers)

### F1. Phantom WorldModel.GetBeliefSnapshotFor + per-friendly snapshot concept (Item 22)
- **Severity**: SEVERE
- **Evidence**: WorldModel.cs has no such method; per-friendly BeliefSnapshot has no architectural existence. Plan self-hedges "verify at impl time."
- **Reviewers**: 10/10
- **Fix**: Drop per-friendly-snapshot premise. Either (a) consume `WorldModel.FusedBuffer` directly with a `EnableSharedBeliefs` policy gate at consumer sites, or (b) design + implement a real per-friendly perception view (LineOfSight-derived, new infrastructure).

### F2. Phantom BeliefState fields (Faction, TechId, HpFraction) (Item 26)
- **Severity**: SEVERE
- **Evidence**: BeliefState.cs:95-160 enumerates all fields. Real names are `Team`, `Id`. HpFraction doesn't exist and P3 doesn't add it to BeliefState.
- **Reviewers**: 10/10
- **Fix**: (1) `b.Team`/`b.Id`/`b.HpFraction` â†’ `b.Team`/`b.Id`/<NEW SOURCE>. (2) For HpFraction, either expand P3 to add HpFraction propagation BlockInstancePose â†’ ArmorMap mean â†’ VehicleModelSnapshot, OR re-source Item 26's HP read from a proxy (e.g., per-tech damage signal already in BeliefState if any, or accept "no damaged-ally detection" v0.2 limitation).

### F3. Phantom TacticalGoalKind enum + wrong ctor shape (Items 26, 28)
- **Severity**: SEVERE
- **Evidence**: TacticalGoal real ctor `(Vector3 pos, float heading, Vector3 vel, float lookAheadSec)` per CostFunction.cs:20. All live GoalSources (Hunter, Gatherer, AircraftSupport) use this 4-arg ctor.
- **Reviewers**: 10/10
- **Fix**: Rewrite both Items 26 and 28 sketches to use canonical `new TacticalGoal(goalPos, heading, velocity, LookAheadSeconds)` form per HunterGoalSource.cs:92.

### F4. Phantom TacticalOptimizer.Step signature in Item 27 handle (Item 27)
- **Severity**: SEVERE
- **Evidence**: TacticalOptimizer.cs:59 â€” `Step(BeliefState, VehicleModelSnapshot, BeliefSnapshot)`. REV 4 omits VehicleModelSnapshot, substitutes IdentityContext.
- **Reviewers**: 10/10
- **Fix**: Handle type â†’ `Func<BeliefState, VehicleModelSnapshot, BeliefSnapshot, TacticalGoal>`. Thread vehicle through `GenericGoalSource.Produce` per tick (Produce already receives vehicle in `(ownBelief, vehicle, beliefs, ctx)` signature).

### F5. Item 27 wiring location mismatch (BuildIdentityContext on SmartPerTechState, not ContinuousController)
- **Severity**: SEVERE
- **Evidence**: SmartRuntime.cs:117-128 owns BuildIdentityContext. SmartPerTechState has no reference to ContinuousController's private `_tactical`. REV 4 hand-waves the cross-class plumbing.
- **Reviewers**: 6/10
- **Fix**: Either (a) move IdentityContext construction onto ContinuousController, OR (b) add a public handle accessor on ContinuousController and have SmartPerTechState read it during BuildIdentityContext, OR (c) inject the handle into SmartPerTechState's ctor at runtime construction.

### F6. Item 27 dispatch simplification silently introduces zero-goal failure mode
- **Severity**: MEDIUM
- **Evidence**: REV4:1163 deletes the `else { _tactical.Step }` fallback. Null `src` â†’ `default(TacticalGoal)` â†’ tech drives to (0,0,0). NaN-guard at ContinuousController.cs:229 doesn't catch zero.
- **Reviewers**: 7/10
- **Fix**: Either keep the inline fallback to `_tactical.Step` when src is null, OR add registration-presence assertion at SmartRuntime.Init that throws if Generic is not registered, OR change NaN-guard to also catch all-zero goals.

### F7. EBasePurpose vs BasePurpose typo (Item 26)
- **Severity**: MEDIUM (compile-blocker but trivially fixable)
- **Evidence**: EBasePurpose.cs:30 declares `public enum BasePurpose`. SmartIdentityClassifier.cs:42-77 uses `BasePurpose.X`.
- **Reviewers**: 9/10
- **Fix**: `EBasePurpose.HarvestingNoHQ` â†’ `BasePurpose.HarvestingNoHQ`. Add null-guard: `purposes != null && purposes.Contains(BasePurpose.HarvestingNoHQ)`.

### F8. Item 26 classifier row uses `armed/mobile/aircraftLike` locals declared inside Hunter row
- **Severity**: MEDIUM (compile-blocker, requires hoist)
- **Evidence**: SmartIdentityClassifier.cs:126-128 declares these locals at Hunter scope. RepairSupport row inserted "before Hunter" cannot see them.
- **Reviewers**: 7/10
- **Fix**: Hoist `armed/mobile/aircraftLike` computations above the RepairSupport row, OR duplicate the predicate expressions inline in the RepairSupport row.

### F9. Item 22 cadence mismatch (SmartForm.Operations is per-tech, not per-team)
- **Severity**: MEDIUM
- **Evidence**: SmartForm.cs:346 â€” Operations runs per-helper. Plan asserts "per-team tick" with no Interlocked gate. ObserveWorldTechsIfDue precedent uses CompareExchange â€” Item 22 doesn't.
- **Reviewers**: 6/10
- **Fix**: Add an Interlocked.CompareExchange interval guard per team, OR find a real per-team main-thread site, OR move RefreshSharedBeliefs into an existing per-team worker site.

### F10. P3 â†’ Item 26 cross-phase phantom (P3 doesn't add HpFraction to BeliefState)
- **Severity**: SEVERE (already covered in F2 but flagged distinctly as a process failure â€” REV 4 invented a cross-phase dependency)
- **Evidence**: REV4:1058 claims "BeliefState.cs â€” P3 adds HpFraction field as part of Item 5 ArmorMap migration." P3 spec at REV4:386 only adds HpFraction to BlockInstancePose.
- **Reviewers**: 10/10
- **Fix**: Either explicitly add a P3 sub-step that propagates HpFraction up to BeliefState (defines aggregation source, publish path, observation model), OR re-source Item 26's HP read (see F2).

---

## 8. Notable Singletons (1-2 reviewers, high credibility)

- **Item 24 XZ projection flattens aircraft altitude** (1 reviewer): PerimeterCoverage uses XZ projection only; two aircraft hovering at different altitudes above same XZ register as clustered. Cosmetic at default-zero weight, but worth noting if WPerimeterCoverage is ever non-zero with aircraft-heavy strategies.
- **Item 24 inline comment incoherence** (1 reviewer): Comment says "mean cos-similarity" but math computes 1 - mean-resultant-length (different concept). Fix the comment, not the math.
- **Item 22 Dictionary race condition** (2 reviewers): `_teamSharedBeliefs` is a plain `Dictionary<TKey,TVal>` cleared+repopulated per main-thread tick while workers iterate. Even at default-OFF the field is allocated; at default-ON there's a real race. Plan defers DoubleBuffer wrap to v0.3 â€” itself a deferment that contradicts the no-deferment headline.
- **Item 26 Hunter fallback contaminates RepairSupport telemetry** (2 reviewers): When no damaged ally found, RepairSupport-tagged techs route through HunterGoalSource. IdentityOutcome routing tags these ticks as RepairSupport behavior even though the goals are literally Hunter outputs. Contaminates Item 21 learning data.
- **Item 27 readonly struct copy bloat** (1 reviewer): IdentityContext is passed by value through the dispatch chain; adding a Func<> field adds 8 bytes per copy.
- **Item 26 ChassisCapture timing** (1 reviewer): If HpFraction is captured at chassis-build time only (spawn / on block change), it's effectively always 1.0 for live techs because there's no re-capture pipeline. "lowestHp ally" selection would never fire even if the field existed.
- **Item 22 conceptual redundancy** (2 reviewers): Even if `GetBeliefSnapshotFor` existed and returned a real per-friendly snapshot, the per-friendly view would just mirror FusedBuffer (since perception is fused team-wide). The "union per-friendly snapshots" loop is a degenerate no-op vs already-existing FusedBuffer.ByTech.

---

## 9. Ship Verdict

**Verdict: NEEDS-REVISION â†’ REV 5 required**

### Trajectory
- **REV 1**: 10/10 NEEDS-REVISION (foundational phantoms throughout)
- **REV 2**: 5/5 split (FATAL vs NEEDS), demotions accepted in REV 3
- **REV 3**: would-have-been **SHIP-WITH-MINOR-FIXES** (per the prior reconciliation), killed by the user's no-deferment directive
- **REV 4**: 5 FATAL + 5 NEEDS-REVISION, 0 SHIP â€” net regression from REV 3 in P3 (cross-phase phantom), P5 (Item 22 phantom), P6 (Items 26+27 phantoms)

### Justification
REV 4 attempted to honor the no-deferment directive by restoring Items 22/26/27, but executed those restorations against a fictional codebase. Of the 4 restorations: 1 (Item 24) ships clean; 3 (Items 22, 26, 27) ship paper code that does not compile against live APIs. The "all 43 materially ship" headline is false â€” effective surface is ~40/43 with 3 broken phantom restorations.

This is worse than REV 3's honest demotions because the failure now masquerades as success: a developer following REV 4 literally would hit compile errors in P5 + P6 and either silently re-defer or redesign mid-implementation. REV 3's honesty table was the better engineering posture.

The honest paths forward:
1. **REV 5 with surgical patches** that ground every restoration in live APIs (Read-verified) â€” see Section 10 below for the concrete patch list.
2. **REV 5 = REV 3 honesty + Item 24 restoration only**, with Items 22/26/27 explicitly deferred to v0.3 with deferment language and revised LOC budgets. This abandons the no-deferment directive but ships REV 4's one real win (Item 24).

The current REV 4 form (paper restoration of broken phantoms) is the worst of both worlds.

---

## 10. If Going REV 5 Surgical-Patch Route â€” Concrete Patch List

### Patch P1: Item 22 redesign (drop phantom per-friendly snapshot)
- Replace `world.GetBeliefSnapshotFor(friendlies[f])` loop with direct read of `world.FusedBuffer.Read().ByTech` (team-wide, already exists).
- Define `RefreshSharedBeliefs` as a copy of `FusedBuffer.ByTech` into `_teamSharedBeliefs` gated by `EnableSharedBeliefs`. This is degenerate vs FusedBuffer directly, so consider whether Item 22 should instead be a CONSUMER-only change: have Hunter/Sniper/Patrol consult `world.FusedBuffer` when own-belief is Lost, gated by the policy flag. No new producer needed.
- Wrap `_teamSharedBeliefs` in `DoubleBuffer<Dictionary<TechId, BeliefState>>` or `ConcurrentDictionary` to avoid the deferred race; do not punt to v0.3.
- Find the real per-team main-thread call site (or use Interlocked-gated single fire); do NOT call from per-tech `Operations`.
- Specify the consumer patches (RoleAssignment.NeedsScout, HunterGoalSource Lost-state fallback) with concrete diffs.

### Patch P2: Item 26 redesign â€” RepairSupportGoalSource
- `b.Faction` â†’ `b.Team` (use TeamId comparison pattern from HunterGoalSource.cs:59: `pair.Value.Team.Equals(ownTeam)`)
- `b.TechId` â†’ `b.Id`
- `b.HpFraction` â†’ **decision required**:
  - Option A: Extend P3 with a new sub-step (Item 5a) that aggregates per-block HpFraction â†’ per-tech mean on `VehicleModelSnapshot.HpFraction` (own-tech only; observe-others is architecturally not feasible without new perception infrastructure). Then read `vehicle.HpFraction` for SELF-protection check; cannot detect damaged ALLIES without new infrastructure.
  - Option B: Accept "no per-tech HP signal in v0.2"; RepairSupport classifier still fires but Produce body only orbits the nearest friendly regardless of HP. Defer "damaged ally selection" to v0.3.
- `new TacticalGoal(TacticalGoalKind.Move, ...)` â†’ `new TacticalGoal(orbitPos, heading, velocity, LookAheadSeconds)` per HunterGoalSource.cs:92 canonical pattern.
- `EBasePurpose.HarvestingNoHQ` â†’ `BasePurpose.HarvestingNoHQ` with null-guard: `purposes != null && purposes.Contains(BasePurpose.HarvestingNoHQ)`.
- Hoist `armed/mobile/aircraftLike` computations above the RepairSupport row insertion point (or duplicate predicates inline).
- Specify Hunter-fallback behavior contamination: either accept it (and document the IdentityOutcome routing distortion) or skip the fallback entirely (return `TacticalGoal.AtCurrent` instead of routing through Hunter).
- Specify exact insertion line in SmartIdentityClassifier (current ambiguity: before Sniper row 5 vs after Sniper row 5).

### Patch P3: Item 27 redesign â€” GenericGoalSource handle
- Handle type â†’ `Func<BeliefState, VehicleModelSnapshot, BeliefSnapshot, TacticalGoal>` (NOT `â€¦, IdentityContext, â€¦`).
- Closure body â†’ `(belief, vehicle, snap) => _tactical.Step(belief, vehicle, snap)`.
- `GenericGoalSource.Produce` body â†’ `return ctx.TacticalOptimizerHandle?.Invoke(ownBelief, vehicle, beliefs) ?? _tactical.Step(ownBelief, vehicle, beliefs);` â€” note the explicit fallback to direct Step rather than `?? default` (prevents zero-goal failure mode).
- Resolve cross-class wiring: pick ONE of:
  - (a) Move IdentityContext construction from `SmartPerTechState.BuildIdentityContext` into `ContinuousController.BuildIdentityContext` (where `_tactical` lives).
  - (b) Add `public Func<...> GetTacticalHandle()` accessor on `ContinuousController`, have `SmartPerTechState.BuildIdentityContext` call it during construction.
  - (c) Pass the handle into `SmartPerTechState` ctor at construction time (back-injected by `ContinuousController`'s owner).
- Specify the SMART-IDENTITY-DESIGN.md sec 7.3 update + the SmartIdentity.cs:80-82 comment update + the DRIFT-DECISIONS.md entry with **actual text**, not just "doc updated."
- Keep the inline `_tactical.Step` fallback in `ContinuousController.cs:212-224` dispatch â€” do NOT simplify to `goal = src?.Produce(...) ?? default;` unless src-null-guard is also added.
- Add a default-OFF policy gate for P7 IdentityOutcome routing of Generic events (silent shift fix).

### Patch P4: Process gates
- Re-apply REV 2's citation-lint rule to ALL new REV 5 sections. The "verify at impl time" hedge at REV4:715 and REV4:1292 must be eliminated â€” every cited file:line and every cited API surface Read-verified before commit.
- Add a "compile-the-sketch" gate: each restoration code block must be tested for `csc` compilation against a stub harness before the plan claims the item ships.
- Update the Item-Accounting Table to honest counts (e.g., "all 43 ship after REV 5 patches land" vs current false "all 43 materially ship").
