# V0.2-PLAN-REV2 Adversarial Review Reconciliation

## 1. Verdict Tally

| Verdict | Count |
|---|---|
| SHIP-AS-IS | 0 |
| SHIP-WITH-MINOR-FIXES | 5 |
| NEEDS-REVISION | 5 |
| FATAL-FLAW-BLOCK-PLAN | 0 |

**Headline: 5/10 SHIP-WITH-MINOR-FIXES vs 5/10 NEEDS-REVISION â€” split decision.** No reviewer called for FATAL-FLAW-BLOCK-PLAN; no reviewer called SHIP-AS-IS. The split hinges on whether the three REV-2-introduced new phantoms (PatrolGoalSource Vector3 return, TunableRegistry.Register signature, TunableRegistry class collision) are "easily fixable in a lint pass" (SHIP-WITH-MINOR-FIXES camp) or "structural defects that prevent clean compile" (NEEDS-REVISION camp).

## 2. Item Accounting Consensus

**10/10 reviewers agree: 43/43 items appear exactly once across P1-P10.** No duplicates flagged. No omissions flagged.

**However, 9/10 reviewers raise the same caveat**: 4 items (22, 24, 26, 27) are "placed" but ship as no-op / drop / enum-slot-only / dropped-entirely:
- Item 22 (TeamBelief.InjectObservations) â€” no-op body
- Item 24 (COverlap defensive perimeter) â€” dropped from P5
- Item 26 (RepairSupport identity) â€” only 1-byte enum slot, classifier+goal source deferred to v0.3
- Item 27 (GenericGoalSource) â€” dropped entirely

**Effective shipped item count: ~39, not 43.** The "43/43 placed" headline is bookkeeping-accurate but materially misleading.

## 3. Per-Phase Consensus Table

| Phase | WELL-FORMED | QUESTIONABLE | BROKEN | Dominant |
|---|---|---|---|---|
| P1 | 10 | 0 | 0 | WELL-FORMED (unanimous) |
| P2 | 5 | 5 | 0 | SPLIT |
| P3 | 10 | 0 | 0 | WELL-FORMED (unanimous) |
| P4 | 7 | 3 | 0 | WELL-FORMED |
| P5 | 4 | 6 | 0 | QUESTIONABLE |
| P6 | 1 | 5 | 4 | QUESTIONABLE/BROKEN |
| P7 | 6 | 4 | 0 | WELL-FORMED |
| P8 | 10 | 0 | 0 | WELL-FORMED (unanimous) |
| P9 | 2 | 6 | 2 | QUESTIONABLE |
| P10 | 1 | 8 | 1 | QUESTIONABLE |

**Cleanest phases:** P1, P3, P8 (unanimous WELL-FORMED).
**Most broken phases:** P6 (4 reviewers called BROKEN over PatrolGoalSource Vector3-vs-TacticalGoal); P9 (2 BROKEN, 6 QUESTIONABLE over TunableRegistry signature); P10 (8 QUESTIONABLE, 1 BROKEN over TunableRegistry namespace collision).

## 4. Phantom Replacement Consensus

| Replacement | Exists | Phantom-Flag | Semantic-Concern Flag |
|---|---|---|---|
| ModuleDrill â†’ BlockDetails.IsMelee | 10/10 | 0/10 | 2/10 (IsMelee tags weapon-melee, not mining-drill â€” approximate) |
| ModuleHealBeam â†’ ModuleShieldGenerator + name-walk | 10/10 | 0/10 | **8/10 (shield â‰  heal; semantic conflation)** |
| ModuleGyro â†’ BlockDetails.IsGyro | 10/10 | 0/10 | 0/10 |
| m_StartHealth â†’ ModuleDamage.maxHealth | 10/10 | 0/10 | 0/10 |

**Headline:** All 4 replacements reference real, verified-live APIs. **No phantom-API issues.** However, **8/10 reviewers raise a serious semantic flag** on ModuleShieldGenerator-as-Repair: a shield generator absorbs damage via energy bubble, it does NOT restore health. Every shield-equipped tech will be flagged as Repair=true, poisoning `BlockKindCounts.Repair` at probe time even though Item 26 (RepairSupport) is deferred to v0.3 with no v0.2 consumer. Recommended fix: rename probe `IsRepair â†’ HasProtection`, or split shield-detection from heal-detection, or gate the bit-set behind a stricter check.

Evidence cited: `Enemy/RCore.cs:23-25/328`, `ManEnemyWorld.cs:2334/2503`, `Templates/RawTechLoader.cs:3265-3275`, `ArchetypeProbe.cs:150` (already used for `BlockKindFlags.Shield`).

## 5. REV 2 Self-Introduced Phantoms (consensus â‰¥3)

### N1. TunableRegistry.Register(string, Func<float>, Action<float>, float, float, float) â€” does not exist
**Reviewers: 9/10** (R1, R2, R3, R4, R5, R6, R7, R8, R9, R10 â€” essentially unanimous)
**Cited at:** `V0.2-PLAN-REV2.md:1224-1232` (P9 C12 hookup), `:1279-1285` (P10 Tooling shape)
**Evidence:** Live `TAC_AI.AI.Tunables.TunableRegistry` (`AI/Tunables/TunableRegistry.cs:13-92`) exposes `RegisterFloat(key, category, defaultValue, min, max, step, applyMode, menuVisible, menuLabel, display, onApply, onDeInit, readSites, bind)` / `RegisterInt(...)` / `RegisterBool(...)` â€” a 14-arg signature with `bind: Action<T>` (setter-only, no getter). The 6-arg `Register(get, set, min, max, def)` signature exists nowhere. P9 cannot compile against the live API as written.

### N2. PatrolGoalSource.Produce returning Vector3 (interface contract violation)
**Reviewers: 9/10** (R1, R2, R3, R4, R5, R6, R7, R8, R9, R10)
**Cited at:** `V0.2-PLAN-REV2.md:865-872` (P6 Item 28 sketch)
**Evidence:** `ISmartGoalSource.Produce` returns `TacticalGoal` per `SmartIdentity.cs:76`. All 6 live implementations (Hunter, Sniper, Base, Gatherer, AircraftHunter, AircraftSupport) return TacticalGoal. The sketched `public Vector3 Produce(...)` would fail to implement the interface and would not compile.

### N3. Second TunableRegistry class at AI/Forms/Smart/Tooling/TunableRegistry.cs â€” name collision with existing
**Reviewers: 8/10** (R1, R2, R3, R5, R6, R7, R8, R10)
**Cited at:** `V0.2-PLAN-REV2.md:1278-1285` (P10 shape)
**Evidence:** Existing `TAC_AI.AI.Tunables.TunableRegistry` already exists and is wired into the in-game UI via `TunableOptionsPublisher` and `AIModuleBootstrap.InitAIModules`. Adding a second class with the identical short name in a different namespace creates ambiguous-reference risk for any file importing both namespaces, and fragments the tunable system. P4 Item 15 itself uses the existing registry for `AetherTunables` â€” the plan is internally inconsistent about which TunableRegistry to use.

### N4. PatrolGoalSource helpers PursueLikeHunter / LissajousAround / FindNearestHostile â€” phantom methods
**Reviewers: 5/10** (R1, R3, R4, R5, R6)
**Cited at:** `V0.2-PLAN-REV2.md:866-872`
**Evidence:** Grep for `PursueLikeHunter`, `LissajousAround`, `FindNearestHostile` returns zero matches across the codebase. `GathererGoalSource:80-95` has spiral-search logic but inlined, not as a reusable helper. `HunterGoalSource` has no `PursueLikeHunter` extract. These are hand-wave method names not specified in the sketch.

### N5. SmartIdentityClassifier.Stamp(SmartIdentity, ...) helper â€” does not exist
**Reviewers: 3/10** (R3, R4, R8)
**Cited at:** `V0.2-PLAN-REV2.md:747, 854` (P6 sketches)
**Evidence:** No `Stamp` method on `SmartIdentityClassifier`. Existing code constructs `new SmartIdentityStamp(identity, fromAuthored: ..., classifiedAtTick: 0L, spawnAnchor: spawnAnchor)` inline at each classifier row. The sketches assume a helper that doesn't exist.

## 6. REV 2 New Silent Shifts (consensus â‰¥3)

### S1. P5 LineOfSightProducer default-OFF FLIPS RoleAssignment.NeedsScout from TRUE-always to FALSE-always
**Reviewers: 3/10** (R5, plus implicit in R1's "Sight=Lost case unaddressed" framing and R4's NeedsScout note)
**Evidence:** v0.1 path at `SmartRuntime.cs:417` populates `losCoverage[kv.Key] = new List<TechId>()` for EVERY tech â€” `NeedsScout` then iterates, finds count==0, returns TRUE always. REV 2 default-OFF: `LineOfSightProducer.SnapshotForTeam` returns empty sentinel â†’ losCoverage dict has zero keys â†’ `NeedsScout`'s foreach has zero iterations â†’ returns FALSE always. The "bit-identical at default" claim is provably FALSE for the scout-role assignment path.

### S2. VelocityAt swap silently changes Sight=Lost case (VelocityMean â†’ Vector3.zero)
**Reviewers: 3/10** (R1, R5, R10)
**Evidence:** `BeliefState.cs:182` returns `Vector3.zero` when `Sight == SightState.Lost`, short-circuiting BEFORE the dt<=0 check. REV 2's dt>0 gate analysis (lines 425-426) misses the Lost-state shift entirely. Gated behind `UseCoastAwareLead=false` default so impact is bounded, but a spawn-test flip would observe Lost targets suddenly losing lead â€” a discrete behavior change.

### S3. CircuitBreaker sentinel-overlay silently overrides any caller passing literal DefaultMaxFailures=10
**Reviewers: 4/10** (R1, R3, R5, R10)
**Evidence:** REV 2 P9 C12 acknowledges this trade-off explicitly. Any future caller (test fixture, new daemon) that intends to pass `10` as an explicit literal will silently inherit AetherTuning.CircuitBreakerMaxFailures, which may have been retuned. The `CreateTuned` factory alternative is mentioned but not shipped.

### S4. P2 DrillRepairProbe.IsRepair sets Repair=true for every shield-equipped tech (semantic poisoning)
**Reviewers: 8/10** (overlaps with Â§4 above) â€” listed here because the bit IS set at probe time even though no v0.2 consumer reads it, leaking phantom semantics to v0.3 RepairSupport consumers.

## 7. Convergent Flaws (â‰¥3 reviewers)

| # | Severity | Where | Evidence | Reviewers | Fix |
|---|---|---|---|---|---|
| **CR1** | **SEVERE** | P6 Item 28: PatrolGoalSource.Produce returns Vector3 | `V0.2-PLAN-REV2.md:865`; `ISmartGoalSource.Produce` returns TacticalGoal per `SmartIdentity.cs:76`; verified across all 6 live GoalSources | 9/10 | Change return type to TacticalGoal; inline spiral logic since LissajousAround/PursueLikeHunter helpers don't exist |
| **CR2** | **SEVERE** | P9 C12 + P10: TunableRegistry.Register(get, set, min, max, def) signature does not exist | `V0.2-PLAN-REV2.md:1224-1232, 1279-1285`; live `AI/Tunables/TunableRegistry.cs:13-92` has only RegisterFloat/Int/Bool with bind:Action<T> | 9/10 | Use existing `TunableRegistry.RegisterInt(key, category, default, min, max, step, bind: v => ...)` API; OR move TunableRegistry creation to P4 to land before P9 |
| **CR3** | **MEDIUM** | P10: Second TunableRegistry class collides with existing TAC_AI.AI.Tunables.TunableRegistry | `V0.2-PLAN-REV2.md:1278-1285`; existing class fully wires in-game UI via TunableOptionsPublisher | 8/10 | Extend the existing registry rather than create a parallel class; OR rename to SmartPolicyRegistry / SmartToolingRegistry |
| **CR4** | **MEDIUM** | P2: ModuleShieldGenerator as Repair-indicator is semantically wrong (shield â‰  heal) | `V0.2-PLAN-REV2.md:262`; ModuleShieldGenerator absorbs damage, doesn't restore HP; Open Question 10 admits the real repair type is unknown | 8/10 | Rename probe IsRepair â†’ HasProtection; OR split IsShield/IsRepair; OR gate bit-set behind stricter check; OR defer Item 4 (DrillRepairProbe) to v0.3 alongside Item 26 |
| **CR5** | **MEDIUM** | P10 csproj timing contradiction unaddressed (carried from REV 1) | REV 1 review Â§5 flagged; each phase ships csproj entries AND P10 says "add all P1-P9 new files"; REV 2 P10 line 1310 retains contradiction | 5/10 | Reframe P10 as "ensures every preceding phase's csproj additions are in place + adds its own 13" |
| **CR6** | **MEDIUM** | P9 PathRequest.Priority "default 128" incoherent on readonly struct (carried from REV 1) | `V0.2-PLAN-REV2.md:1158`; PathRequest is readonly struct at `PathingService.cs:17`; C# 7.3 forbids field initializers on readonly struct fields; default for byte is 0 | 5/10 | Either set in ctor body (signature change + caller update), use 0 default, or use separate ctor overload |
| **CR7** | **MEDIUM** | P5 storage-location internal contradiction within Item 23 | `V0.2-PLAN-REV2.md:608-619`; line 613 surface is `static SnapshotForTeam(TeamId)` on LineOfSightProducer, line 619 recommends TeamRuntime-hosted DoubleBuffer; two ownership models for same data | 5/10 | Pick one: either static-class-with-internal-team-map, or TeamRuntime-hosted with instance method on TeamRuntime |
| **CR8** | **MEDIUM** | P10 missing P5.1 mid-plan flag-flip commit despite REV 2 acknowledging perceptible combat-feel change | `V0.2-PLAN-REV2.md:637` "perceptible combat-feel change requiring playtest gate" vs line 1349-1355 commit list jumping P5 â†’ P6 | 5/10 | Add P5.1 commit for LineOfSightProducer.Enabled flip + spawn-test |
| **CR9** | **MEDIUM** | Citation drift inside REV 2's own C16 corrections: EventBus.cs:148-152 cited for copy-on-write iteration is Subscribers<TEvent> declaration (actual iteration at 199-209) | `V0.2-PLAN-REV2.md` P4 Item 11; live `EventBus.cs:148-152` is `Subscribers<TEvent>` static class decl; iteration at lines 196-209 | 4/10 | Correct citation to 196-209 |
| **CR10** | **MEDIUM** | P2 Item 4 (DrillRepairProbe) ships with zero v0.2 consumers (Item 25 dropped Drill bit; Item 26 deferred Repair) | `V0.2-PLAN-REV2.md:245-277`; P6 Item 25 uses Conveyor/Holder; Item 26 enum-slot-only | 3/10 | Either defer Item 4 to v0.3 alongside Item 26, OR gate [PROBE-DISCOVERY] logging behind dev flag to suppress log spam |
| **CR11** | **MINOR** | "43/43 items placed" headline misleading: items 22, 24, 26, 27 are no-op / drop / enum-slot-only / dropped-entirely | Phase table line 49 + Total line 55; verified across all reviewers | 9/10 | Add footnote: "39 functional + 1 enum-stub + 2 no-op + 1 dropped = 43 placement-count" |
| **CR12** | **MINOR** | TeamSnapshot ctor cascade not enumerated for P7 TeamId addition | `Coordinator.cs:167-189` (5-arg ctor); only caller at `SmartRuntime.cs:428` but P7 doesn't list this | 3/10 | Enumerate the single call site as edit target |
| **CR13** | **MINOR** | AetherTuning doc-vs-code mismatch ("OFF by default" vs 0.35f) papered over via "fix the doc" instead of resolving design intent | `AetherTuning.cs:11` vs `:29`; Open Question 1 admits but defers | 4/10 | Confirm with user whether intended runtime behavior is OFF (0.0f) or ON (0.35f); update whichever side is wrong |
| **CR14** | **MINOR** | RWeapSetup citation drift in C16 table â€” references 52-66 for GimbalAimer + RotateSpeed gate but RotateSpeed gate is at line 89 | `V0.2-PLAN-REV2.md` C16 row; live `RWeapSetup.cs:52-66` is CircleRange branch, `:89` is m_RotateSpeed check | 3/10 | Correct citation to include line 89 |
| **CR15** | **MINOR** | P3 BlockArchetype/BlockInstancePose ctor cascade not enumerated (SpecHP/HpFraction field additions) | `BlockArchetype.cs:14-43` sealed with readonly fields + all-args ctor; affects ChassisCapture.cs:56, ArchetypeProbe.cs:158, Unknown sentinel | 3/10 | Enumerate the 3 ctor-callsites that need update |

## 8. Notable Singletons (1-2 reviewers, high credibility)

| Finding | Reviewers | Why credible |
|---|---|---|
| **P4 Item 14 TechLost ordering ambiguity**: insertion at AetherFuser.cs:101 fires AFTER PublishPerTechBelief at line 100, so subscribers may see new (Lost) belief before/at same time as TechLost event | R5 | Verified by line-level analysis; subtle but real ordering invariant |
| **P5 MainThreadTick called unconditionally even when Enabled=false** (allocator behavior diverges from v0.1 even on OFF path) | R5 | Code-grounded; if any allocation precedes the Enabled check, "bit-identical" claim is false |
| **P9 â†’ P4 fields couple Threadingâ†’World namespace** (CircuitBreakerMaxFailures landing on AetherTuning instead of a new ThreadingTuning.cs) | R3 | Architecturally clean call: AetherTuning XML doc says "belief-subsystem runtime tunables" â€” Threading config is out of scope |
| **P4 IntentRegistry/DamageHintBuffer Forget double-call** (SmartRuntime.Deregister:640 calls World?.DeregisterTech, so hooking both creates idempotent-but-wasteful double Forget) | R3, R6 | Verified `SmartRuntime.cs:637-651`; documented as defense-in-depth but should be explicitly idempotent-by-design |
| **P4 Item 15 bind(0.35f) at register-time overwrites tuned values at boot** (player tuning of VelocitySmoothingStrength in v0.1 gets reset to 0.35f on P4 boot before persisted-value reload) | R3 | Verified CombatTunables.cs:17-22 bind-invocation pattern; not bit-identical for users who tuned values |
| **P2 EmitterFlags.Spooling = 1<<3 conflicts with Wing=1<<4 in adjusted enum** (REV 2 didn't disclose adjusted enum members) | R5 | Verified flag-bit collision possible if added naively |
| **P2 ThrustEmitter FanSpinThreshold field addition** is a readonly-struct ctor break flagged by REV 1 review, NOT addressed in REV 2 P2 | R5, R10 | REV 1 review Â§5 single-reviewer finding; REV 2 doesn't acknowledge or fix |
| **P7 OnPlanPublished signature inconsistency** between sketch (`StrategicPlan prior, StrategicPlan next`) and C10 fix (`PlanType` enums + reward + tickMono) | R8 | Verified `V0.2-PLAN-REV2.md:957` vs `:1027`; would surface at implementation |
| **P5 cross-phase claim contradiction**: P5 line 648-649 says "PatrolGoalSource depends on real LOS coverage" but P6 line 887 says "P5 LineOfSightProducer dependency is NOT load-bearing" | R1, R6 | Verified both statements present in REV 2; self-contradiction |
| **P9 AetherTuning.CircuitBreakerMaxFailures/WindowMs fields don't yet exist** (REV 2 implicitly adds them but doesn't label as NEW) | R3, R4, R6 | Forward-staged, not phantom; minor disclosure gap |

## 9. Ship Verdict

**NEEDS-REVISION (REV 3 lint pass required before P1 commits).**

The split decision (5 SHIP-WITH-MINOR-FIXES vs 5 NEEDS-REVISION) reflects genuine ambiguity, but the convergent evidence tilts toward **REV-3-required**:

1. **CR1 (PatrolGoalSource Vector3 return)**: 9/10 reviewers flag this. If implemented verbatim, P6 push fails compilation â€” the file MUST compile even with default-OFF tunable.
2. **CR2 (TunableRegistry.Register signature phantom)**: 9/10 reviewers flag this. P9 cannot compile as written against the live API. This creates a P9â†’P10 dependency cycle.
3. **CR3 (TunableRegistry class collision)**: 8/10 reviewers flag this. Even if compilable via namespace qualification, it fragments the tunable framework and creates maintenance hazard.
4. **CR4 (ModuleShieldGenerator semantic conflation)**: 8/10 reviewers flag this. Even with Item 26 deferred, the Repair bit gets poisoned at probe time.

These four flaws cannot be deferred to "lint during implementation" because CR1+CR2 are hard compile-fails and CR3+CR4 are architectural decisions that need explicit resolution before code lands.

**Recommended REV 3 scope (small):**
1. Fix `PatrolGoalSource.Produce` return type to `TacticalGoal`; inline spiral logic since helpers don't exist
2. Rewrite P9 C12 to use live `TunableRegistry.RegisterInt(...)` API with `bind: Action<int>` pattern
3. Drop P10's NEW TunableRegistry class; route all v0.2 tunables through the existing `AI/Tunables/TunableRegistry`
4. Rename P2 `DrillRepairProbe.IsRepair` â†’ `HasProtection`, OR defer Item 4 to v0.3 alongside Item 26
5. Address PathRequest.Priority readonly-struct issue (CR6, carried from REV 1)
6. Add P5.1 mid-plan flag-flip commit OR explicitly fold LOS flip into P10 with rationale
7. Resolve P5 storage-location contradiction (LineOfSightProducer-static vs TeamRuntime-hosted)
8. Reconcile P10 csproj timing contradiction (CR5, carried from REV 1)
9. Reframe "43/43 placed" headline with effective-shipped-count footnote
10. Fix EventBus.cs:148-152 citation to 196-209 (CR9)

After these targeted fixes, the plan should be SHIP-WITH-MINOR-FIXES with no further revision required.

## 10. REV 1 â†’ REV 2 Flaw Remediation Audit

| Flaw | Description | Status | Notes |
|---|---|---|---|
| **C1** | SmartEventBridge.cs:244 vs :320 misattribution (DamageObserved vs IdentityOutcome) | **FIXED** | REV 2 correctly identifies :320 for DamageObserved publish, :244 for IdentityOutcome; verified |
| **C2** | VelocityAt swap not bit-identical at dt>0 | **FIXED (partial)** | Default-OFF gate via `AetherTuning.UseCoastAwareLead=false` preserves v0.1; HOWEVER Lost-state shift (zero vs VelocityMean) unaddressed â€” see S2 |
| **C3** | WorkerPool MaxWorkers=22 phantom cap | **FIXED** | `WorkerPool.cs:43-45` "No hard upper cap" verified; REV 2 correctly deletes cap |
| **C4** | P10-only spawn-test gate dishonest (per-phase gating needed) | **FIXED** | Per-phase gates P1.1/P3.1/P4.1/P6.1 added; HOWEVER P5.1 missing despite LOS combat-feel change â€” see CR8 |
| **C5** | GenericGoalSource architectural impossibility (TacticalOptimizer per-instance Adam state) | **FIXED** | Item 27 dropped entirely; `ContinuousController.cs:97` per-instance state verified |
| **C6** | LineOfSight worker-thread Physics.Raycast crash | **FIXED** | P5 reshape to main-thread producer via Operations interval-gate pattern; verified `SmartForm.cs:605-612` clone |
| **C7** | Item 27 GenericGoalSource singleton fundamentally broken | **FIXED** | Dropped (same as C5) |
| **C8** | Item 24 COverlap collides with existing TargetAssignment_Hungarian.COverlap | **FIXED** | `TargetAssignment.cs:42` collision verified; Item 24 dropped/deferred |
| **C9** | Item 25 Gatherer classifier rewrite uses phantom modules | **FIXED** | Rewritten to use live `vehicle.KindCounts.Conveyor/Holder` per `BlockKindCounts.cs:22-23` |
| **C10** | PlanTransition wiring references private TeamRuntime._lastPlanType from Coordinator | **FIXED** | Coordinator-scoped edge tracker; verified `_lastPlanType` is private to TeamRuntime at `SmartRuntime.cs:283` |
| **C11** | DamageHint.DirectionWorld is hardwired Vector3.zero by SmartEventBridge | **FIXED** | DirectionWorld removed at source per `SmartEventBridge.cs:307-318` evidence |
| **C12** | CircuitBreaker ctor const default-param C# 7.3 compile constraint | **FIXED** | Sentinel-overlay pattern is C# 7.3-legal; preserves const default-param ABI |
| **C13** | WeaponProfile ForwardDirectionLocal swap silent semantic shift | **FIXED** | Gated default-OFF via `WeaponSpecPolicy.UseHonestBarrelGeometry=false`; `WeaponProfile.cs:98` swap site verified |
| **C14** | UseReflectedScalars silent semantic shift | **FIXED** | Gated default-OFF via `WeaponSpecPolicy.UseReflectedScalars=false` |
| **C15** | TripleBuffer `where T : class` constraint mismatch with DoubleBuffer | **FIXED** | Constraint dropped per `DoubleBuffer.cs:14-26` evidence |
| **C16** | Pervasive citation drift across plan | **FIXED (mostly)** | C16 corrections table fixes â‰¥11 drifts; HOWEVER REV 2 introduces NEW drifts (CR9 EventBus.cs:148-152, CR14 RWeapSetup line 89) |

**Singletons from REV 1 review that REV 2 did NOT address (SHIFTED/IGNORED):**

| REV 1 Singleton | Status | Evidence |
|---|---|---|
| PathRequest.Priority readonly-struct "default 128" incoherent | **IGNORED** | `V0.2-PLAN-REV2.md:1158` retains unchanged text; 5/10 REV 2 reviewers re-flag (CR6) |
| P10 csproj timing contradiction (P10 adds all + each phase adds own) | **IGNORED** | `V0.2-PLAN-REV2.md:1310` retains contradiction; 5/10 REV 2 reviewers re-flag (CR5) |
| ThrustEmitter FanSpinThreshold readonly-struct ctor break | **IGNORED** | REV 2 P2 still proposes adding field; 2/10 REV 2 reviewers re-flag |

**Summary: 14 of 16 REV 1 convergent flaws FIXED; 2 FIXED-WITH-NEW-DRIFT (C2 Lost-state, C16 self-drift); 3 REV 1 singletons IGNORED.**

REV 2 is a genuine substantial improvement â€” the architectural impossibilities and phantom replacements are largely resolved. The remaining work is a targeted REV 3 lint pass to fix the 4 critical new phantoms (CR1-CR4) plus the 3 ignored REV 1 singletons.
